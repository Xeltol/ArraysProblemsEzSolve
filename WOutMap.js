/*
  Перебор без использования map()
*/

let new_arr = [
    { type: int, length: 7 },
    { type: char, length: 3 },
    { type: Byte, length: 2 },
];

let typeName = Array.from(typeName, ({type}) => type);
console.log(typeName)
